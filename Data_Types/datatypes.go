package main

import "fmt"

func main() {
	var name string = "Indra"
	var age int = 40
	var isMerried bool = false

	fmt.Printf("My name is %s, and my age is %d, and my marital status is %t", name, age, isMerried)
}